# CacaoFerment 可可发酵箱转入晒场前联检闭环

## 项目目标

CacaoFerment 是面向可可初加工合作社的单节点 Go 后端与浏览器联检控制台，用于管理一次发酵箱批次转入晒场前的质量闭环。系统在锁定时冻结收果地块、品种批次、发酵箱编号、装箱重量、翻堆日程、温度探针、快检板孔位、取样盲码、转晒窗口、温升阈值、切豆评分阈值、毒素阈值、理化阈值、复核人员名单和任务代次；随后推进双人装箱确认、设备与窗口占用、翻堆节点温升覆盖采集、盲码样本切豆评分、毒素读数核验、含水率与 pH 复测、异常箱位复判、独立复核，最终形成可转晒、已转晒、风险隔离或已取消中的唯一结论。SQLite WAL 持久化目录、任务聚合、租约账簿、覆盖格、不可覆盖证据链、适配器调用、幂等记录、审计事件和终局凭据，支持确定性重启恢复。规模规划为 23-27 个生产 Go 文件、约 2400-2700 行有效生产 Go 代码，覆盖至少 catalog、task、lease、evidence、arbiter、store、api、cmd、webembed 等包；前端提供真实联检控制台并通过 HTTP/JSON 接入 Go 后端，包含 package.json、锁文件和固定构建脚本。

## 端到端业务流程

1. 目录初始化：录入虚构收果地块、可可品种规则、允许的发酵箱、温度探针、快检板孔位、转晒窗口、合格复核人员与固定阈值版本。
2. 任务锁定：创建一次转晒联检任务，冻结地块、品种批次、发酵箱、装箱重量、翻堆节点、探针、孔位、盲码、窗口、阈值和任务代次，并一次性获取有效租约。
3. 装箱确认：两名不同且具备资格的人员对箱位和重量确认，操作号同内容重试返回同一结果，内容冲突稳定拒绝。
4. 温升与理化采集：按锁定翻堆节点写入温度、持续分钟、翻堆次数、缺测补测标记、含水率、pH 和游离脂肪酸，全部使用确定整数规则。
5. 盲码证据闭合：盲码样本先记录切豆评分和毒素读数，达到揭示闸门后才映射到箱位，证据版本不可覆盖。
6. 异常复判与独立复核：温升断点、霉变阳性、切豆分歧或理化越界只生成一个当前代次复判证据，随后由两名不同且合格人员复核。至多一个竞争者写入终局。

## 核心组件与职责

1. 可可品种与发酵规则目录：维护地块、品种批次、阈值版本、翻堆模板、允许设备、人员资格和规则快照校验。
2. 发酵转晒任务聚合：管理任务代次、状态机、幂等操作号、阶段推进、人员约束、终局单写屏障和重启恢复重放。
3. 箱位/探针/快检板租约账簿：对发酵箱、温度探针、快检板孔位和转晒窗口执行一次性占用、并发裁定、释放规则和任务代次绑定。
4. 温升节点与理化采集账簿：保存翻堆覆盖格、温度斜率、持续分钟、翻堆次数、补测标记、含水率、pH、游离脂肪酸和整数派生结论。
5. 霉变毒素复判及终局仲裁器：维护切豆评分、毒素读数、盲码延迟揭示、复判代次、独立复核和可转晒凭据生成。
6. Go HTTP API：提供 JSON 接口、稳定错误响应、可控故障适配器、审计查询、静态前端嵌入和健康检查。

## 领域规则与不变量

1. 任务状态只能按待锁定、待装箱确认、设备占用中、温升采集中、切豆评分中、毒素核验中、理化复测中、待独立复核、可转晒、已转晒、风险隔离、已取消推进；终态拒绝任何普通写入。
2. 锁定时校验收果地块与品种批次匹配、发酵箱编号唯一、装箱重量为正、翻堆节点严格递增、盲码唯一、快检孔位合法、转晒窗口未被开放任务占用。
3. 每个发酵箱、探针、快检孔位和转晒窗口在开放任务中最多有一个有效租约；锁定、换探针和启动使用同一事务原子裁定。
4. 温升覆盖格按翻堆节点编号写入，重复节点、缺失节点、非法温度、持续分钟越界、翻堆次数越界和补测规则冲突均不得进入有效格。
5. 温度斜率、含水率、pH、游离脂肪酸、切豆百分比和毒素读数使用固定小数位整数运算，检查长度、符号、除零和溢出。
6. 切豆评分必须保持粒数守恒；发酵不足、紫色豆、霉变豆、虫蛀豆和合格豆总数必须等于锁定样本粒数。
7. 盲码样本到箱位的映射只能在揭示闸门打开后写入一次；旧代次迟到读数保留为审计证据但不参与当前结论。
8. 装箱人员与复核人员不得重合；两名独立复核人员必须不同、合格并绑定当前任务代次。

## 数据模型与持久化

1. RuleSnapshot：rule_version、plot_id、variety_batch_id、turn_nodes、temperature_thresholds、cut_test_thresholds、toxin_thresholds、chemistry_thresholds、qualified_reviewers、fixed_scale。
2. FermentTask：task_id、generation、state、plot_id、variety_batch_id、bin_ids、boxed_weight_grams、drying_window_id、locked_rule_version、created_at_tick、terminal_result。
3. LeaseRecord：lease_id、task_id、generation、resource_type、resource_id、status、acquired_at_tick、released_at_tick、release_reason。
4. OperationRecord：task_id、operation_key、operation_kind、request_hash、response_hash、status_code、created_at_tick，用于幂等重试和内容冲突判定。
5. TurnCoverageCell：task_id、generation、bin_id、turn_node、temperature_centi_c、duration_minutes、turn_count、gap_fill_flag、slope_milli_per_min、valid。
6. BlindSample：blind_code、task_id、generation、sample_size、revealed_bin_id、reveal_tick、reveal_generation、sealed。
7. EvidenceVersion：evidence_id、task_id、generation、evidence_kind、subject_key、version_no、payload_hash、integer_values、accepted、reject_code、created_at_tick。
8. AdapterAttempt：attempt_id、task_id、generation、adapter_kind、target_key、script_step、logical_tick、status、stable_error_code、raw_digest、retry_after_tick。
9. ReviewRecord：task_id、generation、reviewer_id、review_kind、decision、reason_code、created_at_tick。
10. FinalCredential：task_id、generation、credential_id、terminal_state、winner_operation_key、issued_at_tick、leased_window_id、digest。

## 公开接口

1. POST /api/tasks/lock：锁定一次联检任务并获取发酵箱、探针、孔位和转晒窗口租约。
2. POST /api/tasks/{id}/boxing-confirmations：提交双人装箱确认，支持操作号幂等。
3. POST /api/tasks/{id}/start-equipment：启动探针与快检板占用，可返回可审计待重试调用。
4. POST /api/tasks/{id}/turn-readings：提交翻堆节点温升覆盖读数和补测标记。
5. POST /api/tasks/{id}/blind-samples/{code}/scores：提交盲码切豆评分、毒素读数及证据版本。
6. POST /api/tasks/{id}/chemistry-readings：提交含水率、pH、游离脂肪酸固定小数位读数。
7. POST /api/tasks/{id}/rejudgements：对异常箱位、盲码和孔位创建当前代次复判证据。
8. POST /api/tasks/{id}/reviews：提交独立复核；POST /api/tasks/{id}/finalize：竞争写入可转晒、风险隔离或已取消。
9. GET /api/tasks/{id} 与 GET /api/tasks/{id}/audit：返回聚合状态、租约、覆盖格、证据链、适配器尝试和确定排序的拒绝原因。
10. 前端联检控制台：任务锁定表单、状态进度、租约视图、翻堆覆盖表、盲码证据区、复判区、复核与终局按钮，使用 npm ci 的确定构建并由 Go embed 提供静态资源。

## 失败边界

1. 所有写接口在单个 SQLite 事务内完成聚合、租约、证据、幂等和审计写入；失败事务不得留下部分租约、覆盖格、样本揭示或最终结论。
2. 拒绝响应使用稳定错误码，并按地块、发酵箱、翻堆节点、盲码排序返回确定原因。
3. 探针、读板器和水分仪适配器支持拒绝、断连、超时和格式错误脚本；失败只形成待重试尝试记录，不生成合格证据，不提前释放占用。
4. 并发锁定、换探针、启动、复判和终局写入使用事务锁与唯一索引共同裁定，公开测试通过同步屏障复现竞争。
5. 重启恢复从 SQLite WAL 读取任务、租约、幂等记录和证据链；开放任务保持原代次，终态任务保持单一凭据。
6. 算术派生值先在内存完成边界检查，再写入证据版本；长度、符号、除零和溢出失败均不产生派生证据。
7. 迟到读数、旧代次复判、终态后的复核和普通操作进入稳定拒绝路径，状态与终局凭据不变。

## 验收标准

1. 锁定时固定收果地块、品种批次、发酵箱、装箱重量、翻堆日程、探针、快检板孔位、取样盲码、转晒窗口、温升阈值、切豆评分阈值、毒素阈值、理化阈值和任务代次。
2. 每个发酵箱、温度探针、快检板孔位和转晒窗口在开放任务中只能被一个有效任务占用，并发锁定、换探针或启动必须原子裁定。
3. 装箱确认、翻堆采集、盲码揭示与复核必须匹配当前任务代次，相同操作号同内容重试幂等而内容冲突报错，陈旧规则或错误地块不得推进状态。
4. 温升采集必须覆盖锁定翻堆节点，温度、持续分钟、翻堆次数和缺测补测使用确定整数规则，非法读数不得写入有效覆盖格。
5. 切豆评分、含水率、pH、游离脂肪酸和毒素读数全部使用固定小数位整数运算并检查长度、符号、除零和溢出，算术失败不得写入派生证据。
6. 温度探针、毒素读板器或水分仪拒绝、断连、超时、格式错误只能形成可审计待重试调用，重试对象、次数和逻辑时间可确定，不得伪造合格结果或提前释放占用。
7. 出现温升断点、霉变阳性、切豆评分分歧或理化指标越界时只能创建一个带代次的复判证据，复判须覆盖受影响箱位、盲码和快检孔位，旧代次迟到读数保持不可覆盖且不影响当前结论。
8. 全部翻堆节点覆盖、温升曲线达标、切豆评分与理化指标满足锁定阈值、毒素证据闭合，并由两名不同且合格人员独立复核后，竞争成功者才可生成唯一转晒凭据；并发转晒、风险隔离与取消只能形成一个终局，终态后的读数、复判、复核或普通操作一律被拒绝且不改变状态。

## 确定性测试场景

1. lock_test.go：并发锁定同一发酵箱、探针、孔位和转晒窗口时只有一个任务成功，失败响应原因排序稳定。
2. lock_test.go：地块与品种批次错配、重复发酵箱编号、翻堆节点非递增、盲码重复分别返回稳定错误码且无租约残留。
3. idempotency_test.go：相同操作号同内容重试返回同一响应，内容冲突拒绝且不推进状态。
4. collection_test.go：翻堆节点覆盖完整后推进，缺失、重复和非法补测不会写入有效覆盖格。
5. collection_test.go：温度曲线斜率处于阈值边界时按整数规则确定通过或异常，测试包含刚好达标与差一单位。
6. evidence_test.go：切豆评分粒数守恒，任一分类导致总数不等于样本粒数时拒绝且不写派生证据。
7. evidence_test.go：含水率、pH、游离脂肪酸和毒素读数覆盖固定小数位、负数、超长、除零和溢出边界。
8. adapter_test.go：探针、读板器和水分仪按故障脚本产生拒绝、断连、超时、格式错误，重试次数和逻辑时间可精确断言。
9. blindcode_test.go：盲码揭示闸门打开前不可映射箱位，打开后只能揭示一次，旧代次迟到读数保留但不影响当前结论。
10. rejudge_test.go：温升断点、霉变阳性、切豆分歧和理化越界只创建一个当前代次复判证据，代次冲突稳定拒绝。
11. finalize_test.go：两名不同合格人员复核后并发终局竞争只有一个凭据写入，其他竞争者得到已终结错误码。
12. recovery_test.go：在锁定后、采集中、复判后和终局后重启服务，任务状态、租约、幂等结果、证据链和凭据完全恢复。

## 组件追踪关系

1. 可可品种与发酵规则目录覆盖锁定快照、地块品种匹配、阈值版本、人员资格和陈旧规则拒绝。
2. 发酵转晒任务聚合覆盖状态推进、代次匹配、操作号幂等、角色分离、终态拒绝和重启恢复。
3. 箱位/探针/快检板租约账簿覆盖发酵箱、探针、孔位、转晒窗口的一次性占用、并发裁定和失败回滚。
4. 温升节点与理化采集账簿覆盖翻堆节点完整性、温升斜率、补测规则、含水率、pH、游离脂肪酸和整数边界。
5. 霉变毒素复判及终局仲裁器覆盖盲码延迟揭示、切豆评分、毒素读数、复判代次、独立复核和唯一转晒凭据。
6. Go HTTP API 覆盖 JSON 接口、稳定错误响应、适配器故障脚本、审计输出、前端嵌入和确定性公开测试入口。

## 独特性

项目聚焦可可初加工中发酵箱批次进入晒场前的联检闭环，核心复杂度来自可可品种发酵规则、翻堆温升覆盖、盲码切豆评分、霉变毒素快检、理化定点整数复测、一次性转晒窗口租约和终局单写屏障。与前序项目相比，它的业务对象、质量证据、异常复判触发条件和转晒凭据语义均围绕可可发酵工艺展开，能够自然形成约 2500 行 Go 后端、真实持久化、并发状态裁定、外部仪器故障处理和确定性公开测试。
